- Removed `Bash(grep)`/`Bash(rg)` from the strategic-mode permission baseline — they are content-readers (code-reading capability) that defeated the SPEC §3.2/§7.3 guarantee that strategic invocations carry no code-reading tools; the baseline now grants only non-content file ops (#15)
